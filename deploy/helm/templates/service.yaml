apiVersion: v1
kind: Service
metadata:
  name: {{ include "mlops-assignment.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "mlops-assignment.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
