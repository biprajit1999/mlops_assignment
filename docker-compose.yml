version: '3.8'
services:
  ml-service:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models
  mlflow:
    image: mlfloworg/mlflow:latest
    ports:
      - "5001:5000"
    environment:
      - MLFLOW_TRACKING_URI=http://0.0.0.0:5000
